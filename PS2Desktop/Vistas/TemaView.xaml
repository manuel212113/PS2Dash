<UserControl x:Class="PS2Desktop.Vistas.TemaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Animación de rotación para el loader -->
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation 
                Storyboard.TargetName="SpinnerEllipse" 
                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                From="0" 
                To="360" 
                Duration="0:0:1.5" />
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <!-- Contenido principal con temas y slider -->
        <Grid x:Name="mainContent" Margin="30,0">
            <Grid Margin="0,10,0,25">
                <StackPanel Orientation="Horizontal" Cursor="Hand">
                    <TextBlock Text="Temas para OPL" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    <Path Data="M10,6L8.59,7.41L13.17,12l-4.58,4.59L10,18l6-6L10,6z" 
                          Fill="White" Opacity="0.7" Stretch="Uniform" Width="12" Margin="10,4,0,0"/>
                </StackPanel>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="36" />
                </Grid.ColumnDefinitions>

                <!-- ScrollViewer que contiene el WrapPanel de temas -->
                <ScrollViewer x:Name="TemasScrollViewer" Grid.Column="0" VerticalScrollBarVisibility="Hidden" Focusable="True">
                    <WrapPanel x:Name="temesPanel" IsItemsHost="False" />
                </ScrollViewer>

                <!-- Slider vertical para controlar el scroll -->
                <Slider x:Name="VerticalSlider" Grid.Column="1" Orientation="Vertical" Minimum="0" Maximum="0" Value="0"
                        VerticalAlignment="Stretch" HorizontalAlignment="Center" Margin="4,0"/>
            </Grid>
        </Grid>

        <!-- Loader / Spinner -->
        <Grid x:Name="LoaderOverlay" Background="#00000080" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Spinner circular -->
                <Ellipse 
                    x:Name="SpinnerEllipse"
                    Width="50" 
                    Height="50" 
                    Stroke="#0078F2" 
                    StrokeThickness="4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,0,0,20"
                    RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Ellipse.RenderTransform>
                </Ellipse>

                <!-- Texto de carga -->
                <TextBlock 
                    Text="Cargando temas..." 
                    Foreground="White" 
                    FontSize="16"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>

                <TextBlock 
                    Text="Espera un momento por favor" 
                    Foreground="#888E9E" 
                    FontSize="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>