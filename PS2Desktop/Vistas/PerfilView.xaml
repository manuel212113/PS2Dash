<Window x:Class="PS2Desktop.Vistas.PerfilView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Perfil" Height="620" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="#121212" WindowStyle="None"
        ResizeMode="NoResize" PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Storyboard x:Key="SaveGlow">
            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                            From="#0055CC" To="#0077EE" Duration="0:0:1.5"
                            AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="20" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border x:Name="WindowCard" CornerRadius="20" Background="#121212" BorderThickness="1" BorderBrush="#2A2F42"
            Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="WindowScale" ScaleX="0.92" ScaleY="0.92"/>
        </Border.RenderTransform>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" x:Name="HeaderPanel">
                <Grid>
                    <TextBlock Text="Editar Perfil" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <Button Content="✕" Foreground="#888E9E" Background="Transparent" BorderThickness="0"
                            HorizontalAlignment="Right" FontSize="18" Cursor="Hand" Click="BtnCerrar_Click"/>
                </Grid>

                <!-- Current avatar preview with glow ring -->
                <Border x:Name="PreviewGlow" Width="84" Height="84" CornerRadius="42"
                        Background="Transparent" BorderThickness="2" BorderBrush="#0055CC"
                        Opacity="0" HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Border Width="76" Height="76" CornerRadius="38" Background="#1A2238"
                            BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border.Clip>
                            <EllipseGeometry RadiusX="38" RadiusY="38" Center="38,38"/>
                        </Border.Clip>
                        <Image x:Name="CurrentAvatar" Stretch="UniformToFill" Width="76" Height="76"/>
                    </Border>
                </Border>
                <TextBlock x:Name="LblUserName" Foreground="White" FontSize="15" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Nombre de usuario (editable) -->
            <Border Grid.Row="1" Background="#1C2030" CornerRadius="10" Padding="15,12" Margin="0,15,0,0"
                    BorderBrush="#2A2F42" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Nombre:" Foreground="#888E9E" FontSize="13" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="TxtDisplayName" Grid.Column="1"
                             Foreground="White" Background="Transparent" BorderThickness="0"
                             FontSize="14" CaretBrush="#0078F2" MaxLength="20"/>
                    <TextBlock x:Name="TxtCharCount" Grid.Column="2" Text="0/20" Foreground="#5A6A78" FontSize="11" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Avatar grid -->
            <ScrollViewer Grid.Row="2" Margin="0,15,0,0" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="AvatarsGrid">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="AvatarBorder" Width="80" Height="80" CornerRadius="16"
                                    Margin="0,0,12,12" Cursor="Hand"
                                    Background="#1A2238" BorderThickness="2" BorderBrush="Transparent"
                                    MouseDown="Avatar_Click" Tag="{Binding ImageUrl}"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.Clip>
                                    <RectangleGeometry Rect="0,0,80,80" RadiusX="16" RadiusY="16"/>
                                </Border.Clip>
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Image Source="{Binding ImageUrl}" Stretch="UniformToFill" Width="76" Height="76"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Save button -->
            <Button Grid.Row="3" x:Name="BtnSave" Content="Guardar cambios" Height="44" Cursor="Hand"
                    Margin="0,10,0,0"
                    Background="#0055CC" Foreground="White" FontSize="15" FontWeight="Bold" BorderThickness="0"
                    IsEnabled="False" Click="BtnSave_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0066E6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#2A3A58"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
